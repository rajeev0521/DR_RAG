embedding_model: "BAAI/bge-small-en-v1.5"
embedding_dim: 384
batch_size: 32
device: null  # 'cuda' or 'cpu'

vector_store:
  url: "http://localhost:6333"
  use_memory: false  # set true if running tests without Docker

retrieval:
  top_k: 10
  rerank: false

generation:
  model_name: "phi3:mini"
  ollama_url: "http://localhost:11434/api/generate"
  temperature: 0.0
